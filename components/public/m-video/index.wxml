<view class="video-container m-video">
  <video
    muted="{{true}}"
    class="default-video"
    src="{{src}}"
    show-center-play-btn="{{false}}"
    autoplay="{{true}}"
    catch:
  ></video>
</view>