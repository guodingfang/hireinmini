<view class="model-header m-header {{bgColor}}">
	<view class="title">{{ title }}</view>
	<slot name="tips"></slot>
	<view class="more" wx:if="{{more}}" bind:tap="handleMore">
		<text>{{moreText}}</text>
		<image class="icon-more" src="/images/icon/more.png"></image>
	</view>
</view>