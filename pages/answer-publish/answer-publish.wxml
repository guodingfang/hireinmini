<block wx:if="{{!isComplete}}">
  <view class="content">
    <view class="title two-row-overstep">{{ details.qatitle }}</view>

    <view class="input-content">
      <textarea
        class="input-content-val"
        placeholder-class="pl-input"
        placeholder="输入回答描述"
        data-type="content"
        bindinput="onInputChange"
        maxlength="-1"
      />
    </view>


    <view class="upload-container">
      <m-upload
        wx:if="{{selectUploadType !== 'video'}}"
        uploadType="img"
        maxCount="9"
        bind:isUpload="isUpload"
        bind:complete="onUploadComplete"
      />
      <m-upload
        wx:if="{{selectUploadType !== 'img'}}"
        uploadType="video"
        bind:isUpload="isUpload"
        bind:complete="onUploadComplete"
      />
    </view>
  </view>

  <view class="footer">
    <view class="btn" bind:tap="onPublishAnswer">发布回答</view>
  </view>
</block>

<block wx:else>
  <view class="publish-complete">
    <image class="publish-complete-img" src="/images/public/answer-complete.png"></image>
  </view>
  <view class="btn" bind:tap="onComplete">回答完成</view>
</block>